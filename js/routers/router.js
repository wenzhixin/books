/*global define*/
define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var BookRouter = Backbone.Router.extend({
        routes: {

        }
    });

    return BookRouter;
});